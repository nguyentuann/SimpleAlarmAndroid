<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- state_checked sẽ theo Switch, nhưng CardView không có state này,
         nên bạn cần truyền thủ công -->
    <item android:color="@color/secondary" android:state_selected="true"/>
    <item android:color="@color/dark_surface"/>
</selector>
